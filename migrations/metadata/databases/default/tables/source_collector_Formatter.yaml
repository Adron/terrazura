table:
  name: formatter
  schema: source_collector
configuration:
  custom_column_names: {}
  custom_root_fields:
    select: formatters
    select_aggregate: formatter_agg
    select_by_pk: formatter
object_relationships:
- name: Connection
  using:
    foreign_key_constraint_on: connection_id
